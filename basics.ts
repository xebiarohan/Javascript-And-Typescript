const add = (num1: number, num2: number) => {
    return num1 + num2;
}

const number1 = 10;
const number2 = 20;

const result = add(number1, number2);
console.log(result);

let value = "alpha";

 // value = 10;   it will give error because of type-inference